#include <stdio.h>

int main() {
    int a = 5;
    int b = 10;
    if (a > 3 && b < 15) {
        printf("a is greater than 3 and b is less than 15\n");
    } else {
        printf("condition not met\n");
    }
    return 0;
}